<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme used by the app before Compose takes over.
         Material3 Compose handles all theming; this is only the window chrome. -->
    <style name="Theme.RuView" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
